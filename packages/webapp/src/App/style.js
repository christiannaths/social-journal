import styled from 'styled-components/macro';

const Layout = styled.div`
  text-align: center;
`;

const Header = styled.div`
  color: #61dbfb;
`;

const Title = styled.h2``;

const Logo = styled.img`
  max-width: 6rem;
`;

export { Layout, Header, Title, Logo };
